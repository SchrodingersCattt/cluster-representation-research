# Curated ABX4 data assets

This directory contains the minimal release assets used by `manuscript/figures/plot_fig5.py`.
It replaces the original working `ABX4_expdata/` tree with curated CIF, PXRD, and property tables.
